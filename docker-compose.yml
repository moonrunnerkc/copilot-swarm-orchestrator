services:
  swarm:
    build: .
    env_file: .env
    volumes:
      - workspace:/workspace
    working_dir: /workspace
    environment:
      - ANTHROPIC_API_KEY
      - OPENAI_API_KEY
      - GITHUB_TOKEN
      - COPILOT_TOKEN

volumes:
  workspace:
